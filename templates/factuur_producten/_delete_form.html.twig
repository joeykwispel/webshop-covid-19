<form method="post" action="{{ path('factuur_producten_delete', {'id': factuur_producten.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ factuur_producten.id) }}">
    <button class="btn">Delete</button>
</form>
